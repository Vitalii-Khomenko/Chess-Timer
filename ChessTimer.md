1. I need a program in HTML with JavaScript that implements a chess timer. The timer should have two separate timers for each player, and it should allow players to start, pause, and reset the timers. The timers should count down from a specified time (e.g., 5 minutes) and switch between players when one player's timer runs out or when a player presses a button to switch turns.
2. Timer we use for Smartphone, so it should be responsive and easy to use on a small screen. - Samsung A55
3. All code should be in a single HTML file, and it should not rely on any external libraries or frameworks. The design should be simple and intuitive, with clear buttons for starting, pausing, resetting, and switching turns. The timers should be displayed prominently, and the interface should be user-friendly for both players.
4. Time control should be accurate, and the timers should update in real-time. When a player's timer runs out, it should display a message indicating that the player has lost on time. The program should also allow players to set the initial time for each timer before starting the game.
5. Timer used full Screen, and the buttons should be large enough to be easily tapped on a smartphone. The design should be clean and minimalistic, with a focus on functionality and ease of use. The timers should be displayed in a clear and readable format, and the buttons should be clearly labeled for their respective functions.
6. The start logic must trigger the timer of the player who tapped their side. (Added: The user explicitly requested that at the moment of start, pressing a timer starts that specific player's timer, corresponding to their side).
7. Create a README.md file in English for GitHub. 